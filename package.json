{
  "name": "thegio-monorepo",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "start": "npm run start --workspace=web",
    "dev:api": "npm run dev --workspace=api"
  },
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "netlify": {
    "build": {
      "use": "@netlify/plugin-nextjs@false"
    }
  }
}